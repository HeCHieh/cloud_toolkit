# Cloud Toolkit - SSH 部署工具开发计划

## 项目概述

**名称**: Cloud Toolkit  
**类型**: Flutter 多端桌面应用 (macOS / Windows / Linux)  
**功能**: 通过 SSH 上传打包程序并部署到远程服务器

---

## 技术栈选型

| 类别 | 选择 | 理由 |
|------|------|------|
| 框架 | Flutter 3.24.1 | 已安装，多端支持 |
| 状态管理 | Riverpod 2.x + flutter_riverpod | 类型安全，适合异步任务管理 |
| SSH 客户端 | dartssh2 | 纯 Dart 实现，支持密码/密钥认证，SFTP 上传 |
| 本地存储 | Hive | 轻量 NoSQL，适合桌面应用 |
| 桌面通知 | local_notifier | 专为桌面设计，支持 macOS/Windows/Linux |
| UI 风格 | Material 3 | 三平台统一风格，简洁现代 |
| 代码生成 | freezed + json_serializable | 不可变数据模型 |
| 文件选择 | file_picker | 跨平台文件/目录选择 |

---

## 数据模型设计

### 1. DeployTask (部署任务)
\`\`\`dart
@freezed
class DeployTask with _$DeployTask {
  const factory DeployTask({
    required String id,
    required String name,           // 任务名称
    required SshConfig sshConfig,   // SSH 配置
    required String localPath,      // 本地文件/目录路径
    required String remotePath,     // 远程目标目录
    String? preScript,              // 上传前脚本
    String? postScript,             // 上传后脚本
    required DateTime createdAt,
    DateTime? updatedAt,
  }) = _DeployTask;
}
\`\`\`

### 2. SshConfig (SSH 配置)
\`\`\`dart
@freezed
class SshConfig with _$SshConfig {
  const factory SshConfig({
    required String host,
    @Default(22) int port,
    required String username,
    String? password,               // 密码认证
    String? privateKeyPath,         // 私钥文件路径
    String? passphrase,             // 私钥密码（可选）
  }) = _SshConfig;
}
\`\`\`

### 3. ExecutionRecord (执行记录)
\`\`\`dart
@freezed
class ExecutionRecord with _$ExecutionRecord {
  const factory ExecutionRecord({
    required String id,
    required String taskId,
    required DateTime startTime,
    DateTime? endTime,
    required ExecutionStatus status, // pending, running, success, failed
    required String logs,            // 完整日志
  }) = _ExecutionRecord;
}

enum ExecutionStatus { pending, running, success, failed }
\`\`\`

---

## 项目结构

\`\`\`
lib/
├── main.dart
├── app.dart                      # MaterialApp 配置
│
├── core/                         # 核心工具
│   ├── constants/
│   ├── extensions/
│   ├── utils/
│   │   └── ssh_executor.dart     # SSH 执行器核心逻辑
│   └── theme/
│       └── app_theme.dart
│
├── data/                         # 数据层
│   ├── models/
│   │   ├── deploy_task.dart
│   │   ├── ssh_config.dart
│   │   └── execution_record.dart
│   ├── repositories/
│   │   ├── task_repository.dart
│   │   └── execution_repository.dart
│   └── services/
│       ├── ssh_service.dart      # SSH/SFTP 封装
│       ├── notification_service.dart
│       └── storage_service.dart  # Hive 初始化
│
├── providers/                    # Riverpod Providers
│   ├── task_provider.dart
│   ├── execution_provider.dart
│   └── deploy_provider.dart      # 部署状态管理
│
└── ui/                           # 界面层
    ├── pages/
    │   ├── home_page.dart        # 任务列表主页
    │   ├── task_form_page.dart   # 新建/编辑任务
    │   ├── task_detail_page.dart # 任务详情 + 历史
    │   └── log_viewer_page.dart  # 日志查看
    ├── widgets/
    │   ├── task_card.dart        # 任务卡片
    │   ├── status_badge.dart     # 状态徽章
    │   ├── script_editor.dart    # 脚本编辑器
    │   └── ssh_config_form.dart  # SSH 配置表单
    └── dialogs/
        └── confirm_dialog.dart
\`\`\`

---

## 界面设计

### 页面 1: 任务列表 (HomePage)
\`\`\`
┌─────────────────────────────────────────────────┐
│  Cloud Toolkit                        [+ 新建]  │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │ 📦 生产环境部署                          │   │
│  │ user@192.168.1.100:22                   │   │
│  │ /dist → /var/www/app                    │   │
│  │                     [历史] [▶ 部署]      │   │
│  │                     ───────────────      │   │
│  │                     ● 空闲               │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │ 📦 测试环境部署                          │   │
│  │ root@test.example.com:22                │   │
│  │ /build → /home/app                      │   │
│  │                     [历史] [▶ 部署]      │   │
│  │                     ───────────────      │   │
│  │                     ● 运行中 (上传 45%)   │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
\`\`\`

### 页面 2: 新建/编辑任务 (TaskFormPage)
\`\`\`
┌─────────────────────────────────────────────────┐
│  ← 新建任务                           [保存]    │
├─────────────────────────────────────────────────┤
│                                                 │
│  任务名称                                       │
│  ┌─────────────────────────────────────────┐   │
│  │ 生产环境部署                             │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  ── SSH 连接 ──────────────────────────────    │
│                                                 │
│  主机              端口                         │
│  ┌──────────────┐  ┌──────┐                    │
│  │ 192.168.1.1  │  │ 22   │                    │
│  └──────────────┘  └──────┘                    │
│                                                 │
│  用户名                                         │
│  ┌─────────────────────────────────────────┐   │
│  │ root                                    │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  认证方式  ○ 密码  ● 密钥                       │
│                                                 │
│  私钥文件                                       │
│  ┌────────────────────────────────┐ [选择]     │
│  │ ~/.ssh/id_rsa                  │            │
│  └────────────────────────────────┘            │
│                                                 │
│  ── 路径配置 ──────────────────────────────    │
│                                                 │
│  本地路径                                       │
│  ┌────────────────────────────────┐ [选择]     │
│  │ /Users/dev/project/dist        │            │
│  └────────────────────────────────┘            │
│                                                 │
│  远程路径                                       │
│  ┌─────────────────────────────────────────┐   │
│  │ /var/www/app                            │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  ── 脚本配置 ──────────────────────────────    │
│                                                 │
│  上传前执行                                     │
│  ┌─────────────────────────────────────────┐   │
│  │ cd /var/www/app                         │   │
│  │ pm2 stop all                            │   │
│  │                                         │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  上传后执行                                     │
│  ┌─────────────────────────────────────────┐   │
│  │ cd /var/www/app                         │   │
│  │ npm install --production                │   │
│  │ pm2 restart all                         │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
\`\`\`

### 页面 3: 执行历史 (TaskDetailPage)
\`\`\`
┌─────────────────────────────────────────────────┐
│  ← 生产环境部署                      [编辑]     │
├─────────────────────────────────────────────────┤
│                                                 │
│  执行历史                                       │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │ ✓ 2024-01-15 14:30:25                   │   │
│  │   耗时: 45s                         [→] │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │ ✗ 2024-01-15 10:15:00                   │   │
│  │   失败: Connection refused          [→] │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │ ✓ 2024-01-14 18:00:12                   │   │
│  │   耗时: 52s                         [→] │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
\`\`\`

### 页面 4: 日志查看 (LogViewerPage)
\`\`\`
┌─────────────────────────────────────────────────┐
│  ← 执行日志 - 2024-01-15 14:30:25              │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐   │
│  │ [14:30:25] 连接到 192.168.1.100:22...   │   │
│  │ [14:30:26] 认证成功                      │   │
│  │ [14:30:26] 执行上传前脚本...             │   │
│  │ [14:30:27] > cd /var/www/app            │   │
│  │ [14:30:27] > pm2 stop all               │   │
│  │ [14:30:28] 开始上传文件...               │   │
│  │ [14:30:28] 上传: index.js (2.3KB)       │   │
│  │ [14:30:29] 上传: package.json (1.1KB)   │   │
│  │ [14:30:45] 上传完成 (共 156 文件)        │   │
│  │ [14:30:45] 执行上传后脚本...             │   │
│  │ [14:30:46] > npm install --production   │   │
│  │ [14:31:10] > pm2 restart all            │   │
│  │ [14:31:10] ✓ 部署完成                   │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
\`\`\`

---

## 核心实现要点

### 1. SSH 连接 (dartssh2)
\`\`\`dart
// 密码认证
final client = SSHClient(
  await SSHSocket.connect(host, port),
  username: username,
  onPasswordRequest: () => password,
);

// 密钥认证
final keyPair = SSHKeyPair.fromPem(await File(keyPath).readAsString());
final client = SSHClient(
  await SSHSocket.connect(host, port),
  username: username,
  identities: [keyPair],
);
\`\`\`

### 2. SFTP 上传
\`\`\`dart
final sftp = await client.sftp();
final remoteFile = await sftp.open(
  remotePath,
  mode: SftpFileOpenMode.create | SftpFileOpenMode.write | SftpFileOpenMode.truncate,
);
await remoteFile.write(File(localPath).openRead().cast());
\`\`\`

### 3. 执行远程命令
\`\`\`dart
final result = await client.run(script);
final output = utf8.decode(result);
\`\`\`

### 4. 桌面通知
\`\`\`dart
await localNotifier.setup(appName: 'Cloud Toolkit');
final notification = LocalNotification(
  title: '部署完成',
  body: '任务「$taskName」执行成功',
);
await notification.show();
\`\`\`

---

## 实施步骤

### Phase 1: 项目初始化 (预计 30 分钟)
1. [ ] 创建 Flutter 项目，启用桌面支持
2. [ ] 配置 pubspec.yaml 依赖
3. [ ] 设置项目结构

### Phase 2: 数据层 (预计 1 小时)
4. [ ] 定义数据模型 (freezed)
5. [ ] 实现 Hive 存储服务
6. [ ] 实现 Task/Execution Repository

### Phase 3: 核心服务 (预计 1.5 小时)
7. [ ] 实现 SSH 服务 (连接/命令/SFTP)
8. [ ] 实现部署执行器 (SshExecutor)
9. [ ] 实现通知服务

### Phase 4: 状态管理 (预计 1 小时)
10. [ ] 实现 Riverpod Providers
11. [ ] 任务状态管理
12. [ ] 执行状态管理

### Phase 5: 界面实现 (预计 2 小时)
13. [ ] 实现主题配置
14. [ ] 实现任务列表页
15. [ ] 实现任务表单页
16. [ ] 实现历史记录页
17. [ ] 实现日志查看页

### Phase 6: 完善 & 测试 (预计 1 小时)
18. [ ] 错误处理优化
19. [ ] 边界情况处理
20. [ ] 多平台测试

---

## 依赖清单 (pubspec.yaml)

\`\`\`yaml
dependencies:
  flutter:
    sdk: flutter
  
  # 状态管理
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  
  # 数据模型
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  
  # SSH
  dartssh2: ^2.9.3
  
  # 存储
  hive: ^4.0.0
  hive_flutter: ^1.1.0
  
  # 桌面通知
  local_notifier: ^0.1.6
  
  # 文件选择
  file_picker: ^8.0.0
  
  # 工具
  uuid: ^4.4.0
  path: ^1.9.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # 代码生成
  build_runner: ^2.4.9
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  riverpod_generator: ^2.4.0
  hive_generator: ^2.0.1
  
  flutter_lints: ^4.0.0
\`\`\`

---

## 风险 & 注意事项

1. **密钥文件权限**: macOS/Linux 可能需要处理文件读取权限
2. **大文件上传**: 需要实现进度回调，避免 UI 卡死
3. **网络超时**: SSH 连接需设置合理超时
4. **日志大小**: 长时间执行可能产生大量日志，需考虑截断策略
5. **并发执行**: 当前设计不支持同一任务并发执行，需加锁

